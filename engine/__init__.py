# -*- coding: utf-8 -*-
"""AI Debate Room 엔진 패키지. 모듈 의존 순서: config → attachments → cli → contract → plan → evidence → usage
→ prompt → compact → runner → store → console. 밖에서는 facade `debate`(debate.py)로 쓴다."""
