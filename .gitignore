# 가상환경·캐시
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 개인 데이터 — 대화 기록, 구 기록, 개인 UI 설정 (기본값은 app.py DEFAULT_SETTINGS)
chats/
runs/
workspace/
ui_settings.json

# CLI 호출용 작업 폴더는 항상 비어 있어야 한다 (.gitkeep만 추적)
sandbox/*
!sandbox/.gitkeep

# 백업·임시
*.bak
*.tmp

# 실기 프로브 원본 출력
tools/probe_out/
